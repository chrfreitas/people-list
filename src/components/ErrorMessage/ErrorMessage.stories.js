import React from 'react';

import ErroMessage from './ErrorMessage';

export default {
  title: 'ErroMessage',
  component: ErroMessage,
};

const Template = (args) => <ErroMessage />;

export const Default = Template.bind({});
Default.args = {};
